class Activity {
    event: string;
    tooltip: string;

    constructor(event: string, tooltip: string) {
        this.event = event;
        this.tooltip = tooltip;
    }
}

export default Activity;